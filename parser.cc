#include "./parser.h"
#include <bits/stdc++.h>
using namespace std;
#define debug(x) std::cout << #x << " is " << (x) << '\n'

signed main(void)
{
    Parser::LL1 parse("grammar/1.txt");
    return 0;
}
